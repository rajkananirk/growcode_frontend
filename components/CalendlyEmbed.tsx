const calendlyUrl = process.env.NEXT_PUBLIC_CALENDLY_URL || "https://calendly.com/your-handle/30min";

export function CalendlyEmbed() {
  const configured = !calendlyUrl.includes("your-handle");
  return <section className="overflow-hidden rounded-[2rem] border border-white/[0.09] bg-white/[0.04] p-2 backdrop-blur-xl"><div className="flex items-center justify-between px-5 py-4"><div><span className="section-label">Book a meeting</span><h2 className="mt-2 text-2xl font-semibold text-white">Choose a time that works</h2></div><span className="hidden rounded-full border border-emerald-400/20 bg-emerald-400/10 px-3 py-1 text-xs text-emerald-300 sm:block">30 minutes</span></div>{configured ? <iframe src={`${calendlyUrl}?hide_gdpr_banner=1&background_color=0d121d&text_color=e2e8f0&primary_color=3b82f6`} title="Schedule a meeting with Growcode Solution" className="h-[720px] w-full rounded-[1.5rem] bg-white" /> : <div className="grid min-h-[420px] place-items-center rounded-[1.5rem] border border-dashed border-white/10 bg-black/20 p-8 text-center"><div><div className="mx-auto grid h-14 w-14 place-items-center rounded-2xl bg-brand-500/10 text-2xl">◷</div><h3 className="mt-5 text-xl font-semibold text-white">Connect your Calendly calendar</h3><p className="mx-auto mt-3 max-w-md leading-7 text-slate-400">Add your booking URL to <code className="text-brand-300">NEXT_PUBLIC_CALENDLY_URL</code> in <code className="text-brand-300">.env.local</code>, then restart the server.</p></div></div>}</section>;
}
